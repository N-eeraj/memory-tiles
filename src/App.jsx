// component imports
import Grid from '@components/Grid'

// style imports
import style from '@styles/app.module.scss'

const App = () => {
  return (
    <main className={style.app}>
      <Grid />
    </main>
  )
}

export default App
